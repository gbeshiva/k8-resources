apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: roboshop
  project: roboshop
  labels:
    app.kubernetes.io/name: myapp
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
      - containerPort: 80
